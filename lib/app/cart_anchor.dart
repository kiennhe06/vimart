import 'package:flutter/widgets.dart';

/// GlobalKey gắn vào icon giỏ hàng trên bottom nav. Hiệu ứng "bay vào giỏ"
/// (flyToCart) đọc vị trí của phần tử này để biết đích đến của ảnh sản phẩm.
final GlobalKey cartIconKey = GlobalKey();
